# 과제 1 - ShelfLog

책과 영화를 찾아보고, 보고 싶은 작품과 감상한 작품을 기록하는 **ShelfLog** 앱을 완성해 봅시다.

앱에는 책과 영화로 이루어진 고정 카탈로그가 준비되어 있습니다. 사용자는 원하는 작품을 검색하고, 평점과 선택적으로 짧은 메모를 남길 수 있습니다.

써야 하는 코드의 양은 많지 않습니다. 이미 있는 코드를 읽고, AI를 이용해 구현 사항에 맞게 구현하고, 이를 검증하는 과정을 훈련하는 것이 이 과제의 목적입니다.

이 과제의 코드와 UI는 의도적으로 좋지 못하게 구성되어 있습니다. 과제로 부여된 부분 외에 개선하고 싶은 점이 있다면 자유롭게 수정해 보세요.

만일 이해가 되지 않는 부분이 있다면 언제든 #a-질문 채널에 질문해주시면 됩니다.

본 과제의 기한은 9/25 23:59입니다.

## 1. 과제의 결과물

앱을 실행하면 책과 영화가 섞인 카탈로그가 목록으로 보입니다. 각 항목에는 **책/영화 종류, 제목, 저자 또는 감독, 연도, 페이지 수 또는 상영 시간, 존재할 경우 평점과 한줄평**이 표시됩니다.

1. 검색창에 단어를 입력한 뒤 화면의 **검색** 버튼 또는 키보드의 검색 버튼(`imeOption`이라고 합니다)을 누르면, 제목이나, 저자 또는 감독 중 하나에 그 단어가 포함된 항목만 목록에 남습니다. 이때, 빈 문자열("")을 검색하면 모든 항목이 보입니다. 결과는 항상 ID 오름차순으로 표시됩니다.
2. 목록에서 작품 하나를 누르면 해당 작품이 선택되고, 아래의 기록 입력 영역이 활성화됩니다.
3. 평점(1~5)과 선택 사항인 한 줄 메모를 입력한 뒤 **기록 저장**을 누릅니다.
4. 올바른 입력이라면 해당 작품의 기록이 앱 메모리에 저장되고, 목록의 해당 작품 아래에 저장한 평점·메모가 표시됩니다. 같은 작품에 다시 저장하면 기록은 하나만 유지되며 가장 최근 입력으로 바뀝니다. (앱 메모리에 저장된다: 실제로는 전혀 저장되지 않으며, 앱을 껐다 켜면 초기화됩니다.)
5. 평점이 1~5가 아니면 기록은 저장되지 않으며, 이유를 알려 주는 메시지가 표시됩니다. 메모는 비어 있을 수 있습니다.
6. **전체 기록 삭제**를 누르면 확인 다이얼로그가 나타납니다. 취소하면 아무 일도 일어나지 않고, 삭제를 선택하면 모든 작품의 기록이 사라집니다.

카탈로그 자체는 바뀌지 않으며, 감상 기록은 앱이 실행되어 있는 동안에만 유지됩니다.

## 2. 과제 준비

- `assignment-1` 브랜치에서 작업합니다.
- 프로젝트를 Android Studio로 열고 실행해 제공된 화면을 먼저 살펴보세요.
- 아래 `data`, `model`, `assignment` 폴더의 파일만 읽고 수정하면 됩니다.

```text
data/
└── RawContents.kt         # 읽기: 원시 책·영화 데이터
model/
├── ContentListItemUiModel.kt # 읽기: 검색 결과 목록에 표시할 작품 정보
└── SaveReviewResult.kt    # 읽기: 기록 저장 결과
assignment/
├── ShelfLogAssignment.kt  # 구현 1: 데이터 모델, 검색, 감상 기록
└── ContextPractice.kt     # 구현 2: Toast와 확인 Dialog
```

`ShelfLogAssignment.kt` 내부의 데이터 모델과 컬렉션은 자유롭게 설계해도 됩니다. 화면과 연결되는 아래 함수의 이름, 파라미터, 반환 타입을 수정할 때에는 `MainActivity.kt` 등 해당 함수를 호출하는 곳도 함께 수정해야 합니다.

```kotlin
fun search(query: String): List<ContentListItemUiModel>

fun saveReview(
    contentId: Int,
    ratingText: String,
    memo: String,
): SaveReviewResult

fun clearReviews()
```

## 3. 과제

### (1) 콘텐츠 모델 만들기

`RawContents.kt`에는 아직 앱의 모델이 되지 않은 원시 데이터가 들어 있습니다. 모든 값은 문자열이며, 책에는 `author`, `pageCount`가, 영화에는 `director`, `runningTimeMinutes`가 있습니다. 이외의 정보는 공통적으로 갖습니다.

이 데이터를 바탕으로 검색과 기록 기능에 적합한 데이터 클래스를 설계하세요. 필요한 클래스, 인터페이스, 상속 구조, enum class와 컬렉션은 자유롭게 선택할 수 있습니다. 2개 이상을 사용하셔도 좋습니다.
단, 클래스를 만드실 때에는 id를 반드시 `Int` 타입으로 해 주세요.

기록을 영화/책과 함께 저장할 수도, 따로 저장할 수도 있습니다. 어떤 방식이 좋을까요?

### (2) 작품 검색하기

검색어를 받아 카탈로그에서 일치하는 작품을 찾는 기능입니다.
여러분이 작성하실 `search` 함수는 ContentListItemUiModel 타입의 리스트를 반환하며, 다음 조건을 만족해야 합니다.
- `search` 함수는 카탈로그를 ContentListItemUiModel으로 변환해 반환합니다.
- 검색어가 비어 있다면 카탈로그 전체를 반환합니다.
- 검색어가 제목 또는 저자(영화의 경우에는 감독)에 포함되는 작품들을 반환합니다.
- 대소문자를 구분하지 않습니다. 즉, "inTerSt"라고 검색하면 "Interstellar"를 결과에 포함해야 합니다.
- 검색 결과는 항상 ID 오름차순으로 정렬합니다.
- 책은 페이지 수를, 영화는 상영 시간을 함께 표시합니다.


### (3) 감상 기록 남기기

작품마다 다음 정보를 남기는 기능입니다.
- 평점: 1점부터 5점까지
- 한 줄 메모 (선택 사항, 작성하지 않으면 빈 문자열 `""`)

여러분이 작성하실 `saveReview` 함수는 `SaveReviewResult`를 반환하며, 다음 조건을 만족해야 합니다.
- 평점이 1~5 범위의 정수가 아니면 기록하지 않고 `SaveReviewResult.Failure`를 반환합니다.
- 해당 작품의 기록이 없다면 새 기록을 만듭니다.
- 이미 기록한 작품이라면 새 기록을 추가하지 않고 기존 기록을 수정합니다.
- 기록에 성공하면 `SaveReviewResult.Success`를 반환합니다.

### (4) 기록 저장 알림 띄우기

기록에 성공하면 `Toast`로 저장 완료 메시지를 보여 주세요. 메시지 내용은 적절하게 적어 주세요.

`ContextPractice.showSavedToast`에는 `activityContext`와 `applicationContext`가 모두 전달됩니다. 둘 중 **하나만** 사용해 구현하세요. 어떤 Context가 알맞은지, 그 이유를 주석에 적어 주세요.

### (5) 모든 기록 지우기

"전체 기록 삭제"를 누르면 바로 지우지 말고, 먼저 `Dialog`로 확인 다이얼로그를 표시합니다. 내용은 적절히 적어 주세요.

- 취소하면 기록을 그대로 유지합니다.
- 확인하면 `onConfirmed`를 호출합니다. (`onConfirmed()`)

`ContextPractice.showClearConfirmation`에도 `activityContext`와 `applicationContext`가 모두 전달됩니다. 둘 중 **하나만** 사용해 구현하세요. 어떤 Context가 알맞은지, 그 이유를 주석에 적어 주세요.

`clearReviews` 함수는 스켈레톤 코드에서 `Dialog`에게 `onConfirmed`로 전달되며, 호출될 경우 저장된 기록을 전부 삭제합니다.

## 4. 제출하기
- 과제를 완료했다면, 포크하신 본인 원격 저장소의 `assignment-1` 브랜치에서 와플스튜디오 원본 원격 저장소의 `assignment-1` 브랜치로 PR을 날려 주세요.
- 기한은 9/25 23:59입니다.

## 5. Git 활용 팁
작업은 `assignment-1` 브랜치에서 진행합니다. 구현 과정이 드러나도록 기능 단위로 커밋을 남겨 주세요. 커밋을 적절히 분리해서 남기는 지의 여부도 평가 대상입니다.
